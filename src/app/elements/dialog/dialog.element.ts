import { Component, Output, Input, EventEmitter } from '@angular/core';

@Component({
    selector: 'dialog',
    templateUrl: './dialog.element.html',
    styleUrls: ['./dialog.element.css']
})
export class DialogElement {
}